build: false
shallow_clone: false
platform: 'x86'

install:
  - cinst git.install
  - set TEST_VAR=Reboot successful!
  - echo before reboot
  - ps: choco install -y vagrant
  - ps: Restart-Computer -Force      # here the server goes restarting
  - ps: Start-Sleep -s 10

build_script:
  - echo after reboot
  - echo %TEST_VAR%

test: off
deploy: off